description: Invalid input or missing required field
content:
  application/json:
    schema:
      type: object
      properties:
        errors:
          type: array
          items:
            type: string
      example:
        errors:
          - Title is required